spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/harsard/bank-application
          search-paths: config
          default-label: main
          clone-on-start: true
server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include=refresh: